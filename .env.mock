# Template only — Next.js does not load this file automatically. `pnpm dev:mock`
# sets NEXT_PUBLIC_API_MOCKING inline; copy these values into `.env.local` if you
# want them picked up by `pnpm dev` too.
NEXT_PUBLIC_API_URL=http://localhost:3000
NEXT_PUBLIC_API_MOCKING=true
